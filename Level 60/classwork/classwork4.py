# Write what are called class variables

# Class variables in Python are variables that are shared by all instances (objects) of a 
# class.lass variables are shared among all instances of the class and are typically used to store values that should be the same for every instance.