// Create a function that takes 2 String arguments and concatenates them

function concatenateStrings(str1, str2) {
    return str1 + str2;
  }
  
  // Example usage:
  const result = concatenateStrings("Hello, ", "World!");
  console.log(result);  // Output: "Hello, World!"  