// Create an Array with the new keyword and write the elements in the Array function

let myArray = new Array(5); // Creates an array of length 5
console.log(myArray); // Outputs: [ <5 empty items> ]
