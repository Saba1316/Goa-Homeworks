// Create some list, run a for loop and output the values ​​to the console


let fruits = ["apple", "banana", "cherry", "melon", "starwberry"];


for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i]);
}